; Register allocation. Flowra is the dataflow-based allocator from
; TODO/backend/registers/flowra.nw; upstream also had dls.nw (DFS linear
; scan, what Backend.x86 actually used via Ralloc.dls) and ocolorgraph.nw
; (graph colouring), both still under TODO/.
;
; The dependency on cmm_cfg_dataflow is the interesting one: it provides
; both Dataflow (the fixpoint engine) and Live (the liveness analysis
; flowra consumes).
(library
 (name cmm_regalloc)
 (wrapped false)
 (libraries
  cmm_commons2 cmm_commons3 cmm_error cmm_parsing cmm_h_asdl
  cmm_ir cmm_front_target
  cmm_front_nelab cmm_front_cfg cmm_front_zipcfg cmm_cfg_dataflow
  cmm_front_ir cmm_front_last cmm_assembler))
